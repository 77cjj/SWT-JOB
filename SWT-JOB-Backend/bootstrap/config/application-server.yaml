# 兼容旧路径；内容与 application.yaml 相同（由 server.sh 同步覆盖）

rag:
  mcp:
    servers: []

rocketmq:
  consumer:
    listeners:
      knowledge-document-chunk_cg:
        knowledge-document-chunk_topic: false
      message-feedback_cg:
        message-feedback_topic: false

logging:
  level:
    org.apache.rocketmq: WARN
